---
- name: Test playbook for azure-vm-image-facts
  hosts: localhost
  connection: local
  gather_facts: false
  user: ansible
  become: yes

  vars_files:
    - /home/testadmin/env.care.rhel.mocatad.yml

  roles:
    - azure-vm-image-facts
